"""Deprecated code, kept for reference only.

Nothing under `deprecated/` is part of the `sqlassert` package, is installed,
or is exercised by the default test run (`pyproject.toml` scopes pytest to
`tests/`). See `deprecated/unique.py` for why.
"""
